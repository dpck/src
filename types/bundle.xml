<types>
  <type name="BundleConfig" desc="Options for the web bundler.">
    <prop string name="src">
      The entry file to bundle. Currently only single files are supported.
    </prop>
    <prop string name="output">
      The path where the output will be saved.
    </prop>
    <prop string name="tempDir" default="depack-temp">
      Where to save prepared JSX files.
    </prop>
    <prop boolean name="preact" default="false">
      Adds `import { h } from 'preact'` automatically.
    </prop>
    <prop opt string name="debug">
      The name of the file where to save sources after each pass. Useful when there's a bug in GCC.
    </prop>
    <prop opt string name="compilerVersion">
      Used in the display message.
    </prop>
    <prop boolean name="noSourceMap" default="false">
      Disables source maps.
    </prop>
  </type>
</types>