<types namespace="_depack">
  <type name="RunConfig" desc="General options for running of the compiler.">
    <prop opt string name="output">
      The path where the output will be saved. Prints to `stdout` if not passed.
    </prop>
    <prop opt string name="debug">
      The name of the file where to save sources after each pass. Useful when there's a potential bug in _GCC_.
    </prop>
    <prop opt string name="compilerVersion">
      Used in the display message. Obtained with the `getCompilerVersion` method.
    </prop>
    <prop boolean name="noSourceMap" default="false">
      Disables source maps.
    </prop>
  </type>
</types>