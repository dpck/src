<types>
  <type name="RunArgs" desc="General options for running of the compiler.">
    <prop string name="output">
      The path where the output will be saved.
    </prop>
    <prop opt string name="debug">
      The name of the file where to save sources after each pass. Useful when there's a bug in GCC.
    </prop>
    <prop string name="compilerVersion">
      Used in the display message.
    </prop>
    <prop boolean name="noSourceMap" default="false">
      Disables source maps.
    </prop>
  </type>
</types>